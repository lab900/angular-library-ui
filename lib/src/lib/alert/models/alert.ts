/**
 * The types of `lab900-alert`. Expose the enum on your component to bind it, for example `[type]="Alert.Error"`.
 */
export enum Alert {
  Info = 'INFO',
  Warn = 'WARN',
  Error = 'ERROR',
  Success = 'SUCCESS',
}
